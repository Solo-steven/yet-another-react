/**
 * ===============================================
 *              Test Script 
 * -----------------------------------------------
 * lib should act like following:
 *  1. call `createContainer` to create a new container instance.
 *  2. call `render` or call `setState` to re-render.
 *  *** createElement is JSX function ***
 * ==============================================
 */

test('test', () => { document.createElement('div'); });